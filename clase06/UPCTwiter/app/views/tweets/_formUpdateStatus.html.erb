<%= form_for(@tweet, :url => "update_status") do |f| %>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>
